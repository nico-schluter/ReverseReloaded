# exploration/scratch

Standalone Python scripts for experimenting with algorithms and geometry before implementing them in `src/`.

Use plain `.py` files (not Jupyter notebooks). Name scripts descriptively after the problem being explored. Each script should have a brief comment header explaining its purpose.
