# exploration

Reference material: prototypes, proof-of-concept scripts, and abandoned approaches.

**Never import from this directory. Never promote code to `src/` without a clean rewrite.**

Keeping exploratory work here provides useful reference for future development without polluting production code.

## Reverse (reference clone)

`exploration/Reverse/` is excluded from this repo. To restore it locally:

```
git clone https://github.com/phossystems/Reverse.git exploration/Reverse
```
